'use client';

import { CalendarView } from './components/calendar-view'; // CalendarViewをインポート

export default function ScheduleCalendarPage() { // コンポーネント名をScheduleCalendarPageに変更
  return (
    <CalendarView />
  );
}